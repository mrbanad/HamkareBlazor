@namespace HamkareBlazor
@typeparam T
@inherits HamkareComponentBase
@implements IDisposable

<span @onclick="ToggleSortDirection" class="@Classname" style="@Style" @attributes="@UserAttributes">
    @if (!AppendIcon)
    {
        @ChildContent
    }
    @if (Enabled)
    {
        <HamkareIcon Icon="@SortIcon" Class="@SortIconClassname" />
    }
    @if (AppendIcon)
    {
        @ChildContent
    }
</span>
