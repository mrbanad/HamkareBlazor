@namespace HamkareBlazor
@inherits ComponentBaseWithState

@if(!DefaultScrollbar)
{
    @((MarkupString)BuildHamkareBlazorScrollbar())
}

@*Needed for charts, maybe put in bool, IncludeCharts="true"??*@
<style>
    .hamkare-chart-serie:hover {
        filter: url(#lighten);
    }

    .hamkare-chart-serie-hovered @*This is the class that is added to the path when hovered for when a datapoint circle is used to capture the mouse positioning*@
    {
        filter: url(#lighten);
    }
</style>

@((MarkupString)BuildTheme())

