@namespace HamkareBlazor
@inherits HamkareComponentBase

<span @attributes="UserAttributes" class="@Classname" style="@Style">
    @ChildContent
    @if (Visible)
    {
        <span class="@WrapperClass">
            <span role="status" aria-live="polite" aria-label="@BadgeAriaLabel" class="@BadgeClassname" @onclick="this.AsNonRenderingEventHandler<MouseEventArgs>(HandleBadgeClick)">
                @if (!Dot)
                {
                    @if (!string.IsNullOrEmpty(Icon))
                    {
                        <HamkareIcon Icon="@Icon" Class="hamkare-icon-badge" />
                    }
                    else
                    {
                        @_content
                    }
                }
            </span>
        </span>
    }
</span>
