@namespace HamkareBlazor.Internal
@using HamkareBlazor.Extensions

<div class="@Classname">
    @if (!string.IsNullOrWhiteSpace(Text))
    {
        <HamkareText Class="hamkare-input-adornment-text"
                 Color="@Color"
                 tabindex="-1">
            @Text
        </HamkareText>
    }
    else if (!string.IsNullOrWhiteSpace(Icon))
    {
        @if (AdornmentClick.HasDelegate)
        {
            <HamkareIconButton Class="hamkare-input-adornment-icon-button hamkare-no-activator"
                           Icon="@Icon"
                           OnClick="@AdornmentClick"
                           Edge="@(Placement.ToEdge())"
                           Size="@Size"
                           Color="@Color"
                           aria-label="@AriaLabel"
                           tabindex="-1"
                           @onmousedown:stopPropagation />
        }
        else
        {
            <HamkareIcon Class="hamkare-input-adornment-icon"
                     Icon="@Icon"
                     Size="@Size"
                     Color="@Color"
                     aria-label="@AriaLabel"
                     tabindex="-1" />
        }
    }
</div>
