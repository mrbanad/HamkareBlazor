@namespace HamkareBlazor
@inherits HamkareComponentBase

<HamkareElement HtmlTag="@GetHtmlTag()"
            Class="@Classname"
            Style="@Style"
            href="@Href"
            target="@Target"
            PreventDefault="GetDisabled()"
            ClickPropagation="false"
            @attributes="UserAttributes"
            @onclick="OnClickHandlerAsync"
            @bind-Ref="ElementReference"
            tabindex="@(!GetDisabled() ? "0" : "-1")"
            aria-disabled="@(GetDisabled() ? "true" : "false")">

    @if (!string.IsNullOrWhiteSpace(Icon))
    {
        <HamkareIcon Class="hamkare-menu-item-icon"
                 Icon="@Icon"
                 Color="IconColor"
                 Disabled="GetDisabled()" />
    }

    <HamkareText Class="hamkare-menu-item-text"
             Typo="GetTypo()">
        @if (ChildContent is null)
        {
            @Label
        }
        else
        {
            @ChildContent
        }
    </HamkareText>

    @if (ActivatesSubMenu && !HideSubMenuArrow)
    {
        <HamkareIcon Class="hamkare-menu-submenu-icon"
                 Icon="@(RightToLeft ? Icons.Material.Filled.ArrowLeft : Icons.Material.Filled.ArrowRight)"
                 Disabled="GetDisabled()" />
    }
</HamkareElement>
